<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_color"
    tools:context=".MainActivity">


    <TextView
        android:id="@+id/titleTextView"
        android:layout_width="match_parent"
        android:layout_height="70dp"
        android:text="Android Chat Application"
        android:textSize="24sp"
        android:background="@color/colorPrimary"
        android:textColor="@color/title_text_color"
        android:textStyle="bold"
        android:fontFamily="sans-serif-condensed"
        android:textAlignment="center" />

    <EditText
        android:id="@+id/roomNameEditText"
        android:layout_width="270dp"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="@dimen/margin_bottom"
        android:hint="Enter room name to add"
        android:fontFamily="casual"/>
    <Button
        android:id="@+id/addRoomButton"
        android:layout_width="100dp"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_toRightOf="@id/roomNameEditText"
        android:layout_marginBottom="@dimen/margin_bottom"
        android:background="@color/colorPrimary"
        android:text="Add Room"
        android:textAllCaps="false"/>

    <ListView
        android:id="@+id/chatsListView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/roomNameEditText"
        android:layout_below="@id/titleTextView"
        android:layout_marginBottom="@dimen/margin_bottom">

    </ListView>



</RelativeLayout>